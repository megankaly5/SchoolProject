function getRandomTsCode() {
  // Generate a random string using the 'random' package and 'typescript'
  const randomString = `YourRandomCode${new Date().getTime()}`;
  
  // Return the generated random string
  return randomString;
}
