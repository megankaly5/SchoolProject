function randomCode() {
  // Randomly generate some basic code

  const basicCode = `
    function sayHello(name: string) {
      console.log(`Hello, ${name}!`);
    }

    sayHello("World");
  `;

  return basicCode;
}
