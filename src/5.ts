// A simple TypeScript function that takes two numbers as input and returns their sum
function add(a: number, b: number): number {
  return a + b;
}

// Example usage of the above function
console.log(add(3, 5)); // Output: 8
